server:
  port: 8000

spring:
  application:
    name: adtech

  data:
    mongodb:
      database: adtech
      port: 27017
      host: localhost
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

image:
  advertisement:
    default-path: /Users/admin/temp-image/