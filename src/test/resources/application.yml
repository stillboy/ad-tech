server:
  port: 8000

spring:
  application:
    name: adtech

  data:
    mongodb:
      database: adtech
      port: 27017
      host: localhost
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:

image:
  advertisement:
    default-path: /Users/admin/temp-image/

pool:
  bid-rate: 0.6
  date-rate: 0.4