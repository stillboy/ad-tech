server:
  port: 8000
  servlet:
    encoding:
      charset: utf-8
      force: true

spring:
  application:
    name: adtech

  data:
    mongodb:
      database: adtech
      port: 27017
      host: localhost
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:

image:
  advertisement:
    default-path: /Users/admin/IdeaProjects/ad-tech/src/main/resources/static/

pool:
  bid-rate: 0.6
  date-rate: 0.4